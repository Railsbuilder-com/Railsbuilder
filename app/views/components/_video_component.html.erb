<%= render 'shared/component_wrapper', component: component do %>
  <% if component["content"].present? %>
    <div class="aspect-video rounded-xl overflow-hidden shadow-lg">
      <%= raw generate_video_embed(component["content"]) %>
    </div>
  <% else %>
    <div class="bg-gradient-to-br from-slate-100 to-slate-200 border-2 border-dashed border-slate-300 rounded-xl p-12 text-center aspect-video flex items-center justify-center">
      <div>
        <svg class="w-16 h-16 mx-auto mb-4 text-slate-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 10l4.553-2.276A1 1 0 0121 8.618v6.764a1 1 0 01-1.447.894L15 14M5 18h8a2 2 0 002-2V8a2 2 0 00-2-2H5a2 2 0 00-2 2v8a2 2 0 002 2z"/>
        </svg>
        <p class="text-slate-600 font-medium">Add video URL</p>
      </div>
    </div>
  <% end %>
<% end %>
